# Database Configuration
# These are defaults for local development.
# In Docker, environment variables (DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD)
# override these values automatically.
db.host=localhost
db.port=3306
db.name=microclimate_grid
db.user=root
db.password=
